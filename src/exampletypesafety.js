"use strict";
const num = 10;
const str = '10';
const sum = num + Number(str);
const someElement = document.getElementById('some-element');
var classList; // = someElement.classList;
if (someElement) {
    classList = someElement.classList;
}
